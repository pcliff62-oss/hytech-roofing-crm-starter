# Google Cloud Storage
GCS_BUCKET=hytechcrm_app_storage
GCS_PROJECT_ID=profound-jet-471516-n9
GCS_CLIENT_EMAIL=
# Paste the private key with literal \n newlines
GCS_PRIVATE_KEY=
# Optional: public base URL (e.g., https://storage.googleapis.com/hytechcrm_app_storage or a CDN)
# GCS_PUBLIC_BASE_URL=

# Optional: HMAC (S3-compatible) access to GCS instead of JSON key
# If you provide these, the app will use S3 API to upload/delete. Create HMAC keys for the service account.
# https://cloud.google.com/storage/docs/authentication/managing-hmackeys
GCS_HMAC_ACCESS_KEY_ID=
GCS_HMAC_SECRET_ACCESS_KEY=
# Optional overrides for S3 client
# GCS_S3_ENDPOINT=https://storage.googleapis.com
# GCS_S3_REGION=us-east-1

# Optional alternative (instead of the above): path to JSON key file
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account.json

## Public Google Maps JavaScript API key (loaded in browser). Restrict by HTTP referrers.
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Apple MapKit JS (for satellite map)
# MAPKIT_TEAM_ID=
# MAPKIT_KEY_ID=
# Paste the PKCS8 private key (with literal \n)
# MAPKIT_PRIVATE_KEY=
# Or point to a file on disk
# MAPKIT_PRIVATE_KEY_FILE=/absolute/path/AuthKey_XXXX.p8
# MapKit Maps ID (Xcode Maps ID / Maps Identifier)
# MAPKIT_MAPS_ID=

## (Optional) Server-side Google Maps key if you want to keep a private variant.
# GOOGLE_MAPS_API_KEY=

# Field App (Vite) API base URL
# Used by the mobile/field app when running separately via Vite
VITE_API_BASE=https://REPLACE-WITH-YOUR-CLOUD-RUN-URL
