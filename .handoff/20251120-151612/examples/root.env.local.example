REPORT_FORCE_LOCAL=1
GCS_BUCKET=hytechcrm_app_storage
GCS_PROJECT_ID=profound-jet-471516-n9
# Replace with your real HMAC keys locally (placeholders committed)
GCS_HMAC_ACCESS_KEY_ID=__REPLACE_ME_GCS_ACCESS_KEY_ID__
GCS_HMAC_SECRET_ACCESS_KEY=__REPLACE_ME_GCS_SECRET_ACCESS_KEY__
# Optional public base URL for object links (leave commented to default)
# GCS_PUBLIC_BASE_URL=https://storage.googleapis.com/hytechcrm_app_storage

# Apple MapKit JS (placeholders)
MAPKIT_TEAM_ID=__REPLACE_ME_MAPKIT_TEAM_ID__
MAPKIT_KEY_ID=__REPLACE_ME_MAPKIT_KEY_ID__
# Paste your PKCS8 private key here (with literal \n newlines) - do not commit real value
MAPKIT_PRIVATE_KEY=__REPLACE_ME_MAPKIT_PRIVATE_KEY_PKCS8__
# Optional: MapKit Maps ID (create in Apple Dev portal > Identifiers > Maps IDs)
MAPKIT_MAPS_ID=__REPLACE_ME_MAPKIT_MAPS_ID__
# Optional: comma-separated allowed origins override (e.g. http://localhost:3000,http://localhost:3001)
# MAPKIT_ALLOWED_ORIGINS=http://localhost:3000

# Google Maps
GOOGLE_MAPS_API_KEY=__REPLACE_ME_GOOGLE_MAPS_API_KEY__
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=__REPLACE_ME_GOOGLE_MAPS_API_KEY__

# Public base URL for callbacks and absolute links (dev)
PUBLIC_BASE_URL=http://localhost:3000

# Pix4D Cloud API key (trial placeholder)
PIX4D_API_KEY=__REPLACE_ME_PIX4D_API_KEY__

# Mapware Cloud (placeholders)
# MAPWARE_API_KEY=__REPLACE_ME_MAPWARE_API_KEY__
# MAPWARE_API_BASE=https://api.mapware.com

# AI worker (naive single-photo measure) — base URL only
AI_WORKER_URL=http://localhost:8089
